template<typename T>
void bubbleSort(T arr[], int n) {
	//需要排序的次数
	for (int i = 1; i < n ; i++) {
    //将最大的元素排到最后一位
		for (int j = 0; j < n - i; j++) {
			if (arr[j] > arr[j + 1])
				swap(arr[j], arr[j + 1]);
		}
	}
}
